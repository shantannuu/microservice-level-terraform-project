name: Terraform apply

on:
  push:
    branches: [ main ]

jobs:

  # ---------------- DEV ----------------
  apply-dev:
    runs-on: ubuntu-latest
    environment: dev

    defaults:
      run:
        working-directory: live/dev/network/vpc


    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1

    - name: Terraform Init
      run: terraform init

    - name: Download Terraform Plan
      run : |
        aws s3 cp s3://my-terraform-plans-bucket-shantanu/dev/tfplan tfplan

    - name: Terraform Apply
      run: terraform apply -auto-approve tfplan

  # ---------------- STAGE ----------------
  # apply-stage:
  #   needs: apply-dev
  #   runs-on: ubuntu-latest
  #   environment: stage

  #   defaults:
  #     run:
  #       working-directory: live/stage/network/vpc


  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v5

  #   - name: Set up Terraform
  #     uses: hashicorp/setup-terraform@v3

  #   - name: Configure AWS credentials
  #     uses: aws-actions/configure-aws-credentials@v2
  #     with:
  #       aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #       aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #       aws-region: ap-south-1

  #   - name: Terraform Init
  #     run: terraform init

  #   - name: Download Terraform Plan
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: tfplan-stage

  #   - name: Terraform Apply
  #     run: terraform apply -auto-approve tfplan

  
  # # ---------------- PROD ----------------
  # apply-prod:
  #   needs: [apply-stage]
  #   runs-on: ubuntu-latest
  #   environment: prod

  #   defaults:
  #     run:
  #       working-directory: live/prod/network/vpc


  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v5

  #   - name: Set up Terraform
  #     uses: hashicorp/setup-terraform@v3

  #   - name: Configure AWS credentials
  #     uses: aws-actions/configure-aws-credentials@v2
  #     with:
  #       aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #       aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #       aws-region: ap-south-1

  #   - name: Terraform Init
  #     run: terraform init

  #   - name: Download Terraform Plan
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: tfplan-prod

  #   - name: Terraform Apply
  #     run: terraform apply -auto-approve tfplan